{
  "name": "catertracksas-astro",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "node scripts/gen-sitemap.mjs && astro build && pagefind --site dist",
    "preview": "astro preview --host 0.0.0.0",
    "sitemap": "node scripts/gen-sitemap.mjs"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
